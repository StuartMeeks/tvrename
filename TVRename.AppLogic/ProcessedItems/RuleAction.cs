namespace TVRename.AppLogic.ProcessedItems
{
    public enum RuleAction
    {
        Remove,
        Swap,
        Merge,
        Insert,
        IgnoreEp,
        Rename,
        Split,
        Collapse
    }
}
