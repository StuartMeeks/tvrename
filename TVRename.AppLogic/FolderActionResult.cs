namespace TVRename.AppLogic
{
    public enum FolderActionResult
    {
        NotSet,
        Retry,
        Cancel,
        Create,
        IgnoreOnce,
        IgnoreAlways,
        DifferentFolder
    }
}
