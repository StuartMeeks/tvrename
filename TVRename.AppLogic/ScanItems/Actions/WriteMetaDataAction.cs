namespace TVRename.AppLogic.ScanItems.Actions
{
    public interface WriteMetaDataAction
    {
        // empty base interface, just so we can see if an action is one of these or not, for grouping and processing
    }
}
